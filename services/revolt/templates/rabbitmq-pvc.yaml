apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: revolt-rabbitmq-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: {{ .Values.rabbitMQSize }}