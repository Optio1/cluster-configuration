apiVersion: apps/v1
kind: Deployment
metadata:
 labels:
   run: revolt-mongo
 name: revolt-mongo
 namespace: revolt
spec:
 replicas: 1
 selector:
   matchLabels:
     run: revolt-mongo
 template:
   metadata:
     labels:
       run: revolt-mongo
   spec:
     volumes:
      - name: revolt-mongo-pv
        persistentVolumeClaim:
          claimName: revolt-mongo-pvc
     containers:
      - name: revolt-mongo
        image: docker.io/mongo
        ports:
          - containerPort: 27017
        resources:
          limits:
            memory: 1000Mi
          requests:
            memory: 800Mi
            cpu: 100m
        livenessProbe:
          exec:
            command:
              - echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
          initialDelaySeconds: 10
          periodSeconds: 10
        volumeMounts:
          - name: revolt-mongo-pv
            mountPath: /data/db
