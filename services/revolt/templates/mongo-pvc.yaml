apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: revolt-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: {{ .Values.mongoPVSize }}